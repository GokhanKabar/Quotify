<form method="post" action="{{ path('back_company_delete', {'id': company.id}) }}" onsubmit="return confirm('Êtes-vous sûr de vouloir supprimer cet élément?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ company.id) }}">
    {% include 'components/_button.html.twig' with {
        'text': 'Supprimer',
        'bgColor': 'red',
        'textColor': 'white',
    } %}
</form>
