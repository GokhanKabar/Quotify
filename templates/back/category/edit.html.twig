{% extends 'admin.html.twig' %}

{% block title %}Modification d'une categorie{% endblock %}

{% block body %}
    <div class="flex flex-col p-5 gap-5">
        <div class="flex items-center justify-between">
            <div class="flex items-center">
                <h1 class="lg:text-4xl md:text-2xl text-2xl">Modification d'une categorie</h1>
            </div>
            <div class="flex gap-2">
                {{ include('back/category/_delete_form.html.twig') }}
                {% include 'components/_button.html.twig' with {
                    'path': 'back_category_index',
                    'text': 'Retour à la liste',
                    'bgColor': 'blue',
                    'textColor': 'white',
                } %}
            </div>
        </div>
        {{ include('back/category/_form.html.twig', {'button_label': 'Sauvegarder'}) }}
    </div>
{% endblock %}
