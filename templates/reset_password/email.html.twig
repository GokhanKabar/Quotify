<style>
section {
            display: flex;
            flex-wrap: wrap;
            align-items: center;
            position: relative;
        }

        .content-container {
            width: 100%;
            padding: 4rem;
        }

        h1 {
            font-size: 2rem;
            font-weight: bold;
            margin-bottom: 1rem;
        }

        p {
            margin: 1rem 0;
            color: #888;
        }

        a {
            color: #3498db;
            text-decoration: none;
            font-weight: bold;
        }

        img {
            width: 100%;
            height: auto;
        }
    </style>
<div class="content-container">
   <div class="w-full px-4 py-12 sm:px-6 sm:py-16 lg:w-1/2 lg:px-8 lg:py-24">
            <div class="mx-auto max-w-lg text-center">
                <h1 class="text-2xl font-bold sm:text-3xl">Réinitialisation de mot de passe</h1>

            <p>Pour réinitialiser votre mot de passe, veuillez visiter le lien suivant :</p>

                
                <a href="{{ url('app_reset_password', {token: resetToken.token}) }}" class="color: #3498db; text-decoration: none; font-weight: bold;">
                    {{ url('app_reset_password', {token: resetToken.token}) }}
                </a>

                <p>
                    Ce lien expirera dans {{ resetToken.expirationMessageKey|trans(resetToken.expirationMessageData, 'ResetPasswordBundle') }}.
                </p>

                <p>À bientôt !</p>
            </div>
        </div>

</div>