{% extends 'base.html.twig' %}

{% block title %}Email de réinitialisation de mot de passe envoyé{% endblock %}

{% block body %}
    <section class="relative flex flex-wrap lg:h-screen lg:items-center">
        <div class="w-full px-4 py-12 sm:px-6 sm:py-16 lg:w-1/2 lg:px-8 lg:py-24">
            <div class="mx-auto max-w-lg text-center">
                <h1 class="text-2xl font-bold sm:text-3xl">Email de réinitialisation de mot de passe envoyé</h1>

                <p class="mt-4 mb-4 text-gray-500">
                    Si un compte correspondant à votre adresse e-mail existe, un e-mail vient d'être envoyé. Il contient un lien que vous pouvez utiliser pour réinitialiser votre mot de passe.
                    Ce lien expirera dans {{ resetToken.expirationMessageKey|trans(resetToken.expirationMessageData, 'ResetPasswordBundle') }}.
                </p>
                
                <p class="mb-4">Si vous ne recevez pas d'e-mail, veuillez vérifier votre dossier spam ou <a href="{{ path('app_forgot_password_request') }}" class="text-primary hover:underline">réessayer</a>.</p>
            </div>
        </div>

        <div class="relative h-64 w-full sm:h-96 lg:h-full lg:w-1/2">
            <img src="{{ asset('images/password.jpg') }}" class="absolute inset-0 h-full w-full object-cover"/>
        </div>
    </section>
{% endblock %}
