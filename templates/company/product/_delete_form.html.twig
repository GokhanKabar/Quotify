<form method="post" action="{{ path('company_product_delete', {'id': product.id}) }}" onsubmit="return confirm('Êtes-vous sûr de vouloir supprimer cet élément?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ product.id) }}">
    {% include 'components/_button.html.twig' with {
        'text': 'Supprimer',
        'bgColor': 'red',
        'textColor': 'white',
    } %}
</form>
