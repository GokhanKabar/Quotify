<form method="post" action="{{ path('company_quotation_delete', {'id': quotation.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ quotation.id) }}">
    {% include 'components/_button.html.twig' with {
        'text': 'Supprimer',
        'bgColor': 'red',
        'textColor': 'white',
    } %}
</form>
