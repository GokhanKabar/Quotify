<form method="post" action="{{ path('company_quotation_delete', {'id': quotation.id}) }}" onsubmit="return confirm('Êtes-vous sûr de vouloir supprimer cet élément?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ quotation.id) }}">
    {% include 'components/_button.html.twig' with {
        'text': 'Supprimer',
        'bgColor': 'red',
        'textColor': 'white',
    } %}
</form>
