{% extends 'dashboard.html.twig' %}

{% block title %}Création d'un devis{% endblock %}

{% block body %}
    <div class="flex flex-col p-5 gap-5">
        <div class="flex items-center justify-between">
            <div class="flex">
                <h1 class="lg:text-4xl md:text-2xl text-2xl">Créer un nouveau devis</h1>
            </div>
            <div class="flex">
                {% include 'components/_button.html.twig' with {
                    'path': 'company_quotation_index',
                    'text': 'Retour à la liste',
                    'bgColor': 'blue',
                    'textColor': 'white',
                } %}
            </div>
        </div>
        {{ include('company/quotation/_form.html.twig') }}
    </div>
{% endblock %}
